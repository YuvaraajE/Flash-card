language = "python3"
run = "python3 main.py"

# Type the run value in shell and click on the link thats get displayed in it. (email_validator package is required in replit)
# Change the host url in YAML file for API access using OPENAPI specification.